# shellcheck shell=bash # -*- mode: sh; eval: (sh-set-shell "zsh") -*-

if ! [ -x "$(command -v cargo-cache)" ]; then
    cargo install cargo-cache
fi

cargo cache --autoclean --fsck --keep-duplicate-crates 1

rustup update
